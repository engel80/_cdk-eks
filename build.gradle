apply plugin: 'base'
apply plugin: 'org.sonarqube'
archivesBaseName = 'cdk-eks'

buildscript {
    repositories {
        mavenCentral()
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
    dependencies {
        classpath("org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:3.4.0.2513")
    }
}

sonarqube {
    properties {
        property "sonar.projectName", "cdk-eks"
        property "sonar.projectKey", "cdk-eks"
        property "sonar.sourceEncoding", "UTF-8"
        property "sonar.sources", "."
        property "sonar.exclusions", "**/node_modules/**, **/cdk.out/**"
        property "sonar.cpd.exclusions", "**/*index.ts"
        property "sonar.host.url", "http://127.0.0.1:9000"
        property "sonar.links.ci", "https://example.com"
        property "sonar.log.level", "DEBUG"
    }
}
